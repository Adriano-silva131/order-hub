.vscode
.idea

# Binários baixados localmente
kubectl
minikube-linux-amd64

# Tokens JWT gerados pelo create-users.sh — nunca commitar
k6/tokens.json

# Variáveis de ambiente locais do k6 — nunca commitar
k6/.env
